<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ========== ACTION WIZARD (définie avant les menus) ========== -->

    <record id="action_import_irg_bareme_wizard" model="ir.actions.act_window">
        <field name="name">Importer Tableau IRG</field>
        <field name="res_model">import.irg.bareme.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- ========== VUE LISTE BAREME ========== -->

    <record id="view_hr_irg_bareme_tree" model="ir.ui.view">
        <field name="name">hr.irg.bareme.tree</field>
        <field name="model">hr.irg.bareme</field>
        <field name="arch" type="xml">
            <list string="Tableaux IRG">
                <field name="name"/>
                <field name="date_import"/>
                <field name="soumis_min" string="Soumis min (DA)"/>
                <field name="soumis_max" string="Soumis max (DA)"/>
                <field name="line_count" string="Nb lignes"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- ========== VUE FORM BAREME ========== -->

    <record id="view_hr_irg_bareme_form" model="ir.ui.view">
        <field name="name">hr.irg.bareme.form</field>
        <field name="model">hr.irg.bareme</field>
        <field name="arch" type="xml">
            <form string="Tableau IRG">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object"
                                class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button"
                                   options='{"terminology": "archive"}'/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="company_id"/>
                            <field name="date_import"/>
                        </group>
                        <group>
                            <field name="line_count" string="Lignes importées"/>
                            <field name="soumis_min" string="Soumis min (DA)"/>
                            <field name="soumis_max" string="Soumis max (DA)"/>
                        </group>
                    </group>
                    <group>
                        <field name="notes" nolabel="1" placeholder="Notes…"/>
                    </group>
                    <notebook>
                        <page string="Aperçu (50 premières lignes)">
                            <field name="line_ids" readonly="1">
                                <list limit="50">
                                    <field name="soumis" string="Soumis (DA)"/>
                                    <field name="irg_general" string="IRG Cas général (DA)"/>
                                    <field name="irg_particulier" string="IRG Cas particuliers (DA)"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ========== VUE LISTE LIGNES ========== -->

    <record id="view_hr_irg_bareme_line_tree" model="ir.ui.view">
        <field name="name">hr.irg.bareme.line.tree</field>
        <field name="model">hr.irg.bareme.line</field>
        <field name="arch" type="xml">
            <list string="Lignes tableau IRG">
                <field name="soumis" string="Soumis (DA)"/>
                <field name="irg_general" string="IRG Cas général (DA)"/>
                <field name="irg_particulier" string="IRG Cas particuliers (DA)"/>
            </list>
        </field>
    </record>

    <!-- ========== ACTION PRINCIPALE ========== -->

    <record id="action_hr_irg_bareme" model="ir.actions.act_window">
        <field name="name">Tableau IRG</field>
        <field name="res_model">hr.irg.bareme</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Aucun tableau IRG importé
            </p>
            <p>
                Utilisez le menu <strong>Importer Tableau IRG</strong> pour charger
                le fichier CSV extrait du PDF officiel DGI.
            </p>
        </field>
    </record>

    <!-- ========== MENUS ========== -->

    <menuitem id="menu_hr_irg_bareme"
              name="Tableau IRG"
              parent="payroll.payroll_menu_configuration"
              action="action_hr_irg_bareme"
              sequence="30"/>

    <menuitem id="menu_import_irg_bareme"
              name="Importer Tableau IRG"
              parent="payroll.payroll_menu_configuration"
              action="action_import_irg_bareme_wizard"
              sequence="31"/>

</odoo>
